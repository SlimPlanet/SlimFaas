meta {
  name: Async failure
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/async-function/fibonacci1/error
  body: none
  auth: none
}

tests {
  test("Expected HTTP status", function () {
    expect([202]).to.include(res.getStatus());
  });
}

docs {
  Run individually. See https://slimfaas.dev/guided-tour and https://slimfaas.dev/api-reference.
  Intentionally produces an application failure; async delivery can retry.
}
