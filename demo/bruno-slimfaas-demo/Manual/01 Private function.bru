meta {
  name: Private function
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/function/fibonacci4/hello/tour
  body: none
  auth: none
}

tests {
  test("Expected HTTP status", function () {
    expect([200, 404]).to.include(res.getStatus());
  });
}

docs {
  Run individually. See https://slimfaas.dev/guided-tour and https://slimfaas.dev/api-reference.
  External callers normally get 404; native local callers can share trusted loopback addresses and receive 200.
}
