meta {
  name: Missing counter amount
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/data/sets/{{tourId}}-count/incrby
  body: none
  auth: none
}

tests {
  test("Expected HTTP status", function () {
    expect([400]).to.include(res.getStatus());
  });
}

docs {
  Run the Tour folder in order. See https://slimfaas.dev/guided-tour and https://slimfaas.dev/api-reference.
}
